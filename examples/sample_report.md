# Example

```bash
./jsentinel -site https://target.tld -same-host -max-pages 10 -max-js 30 -md-out report.md
```

ثم افتح `report.md` لمراجعة:
- endpoints
- API paths
- source maps
- dangerous sinks
- suspected secrets
